from typing import List, Dict, Any

def chunk_document(doc: Dict[str, Any], chunk_size: int = 1000, chunk_overlap: int = 200) -> List[Dict[str, Any]]:
    """Splits a cleaned document into smaller, overlapping chunks with metadata."""
    # TODO: Implement text and table-aware chunking strategies
    return []
